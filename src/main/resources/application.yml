spring:
  datasource:
    url: jdbc:h2:tcp://10.2.0.51:8082/~/lala;DB_CLOSE_DELAY=-1 #jdbc:h2:tcp://localhost/~/nodeDB #;MODE=LEGACY
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace #??? ?? 3.x, hibernate6
  org.springframework.transaction: DEBUG